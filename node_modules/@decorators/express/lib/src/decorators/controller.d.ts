import { Type } from '../middleware';
/**
 * Registers controller for base url
 *
 * @param {string} url
 * @param {Type[]} [middleware]
 */
export declare function Controller(url: string, middleware?: Type[]): (target: any) => void;
